export type VectorObject = {
    x: number;
    y: number;
    z?: number;
};
export type PrintSettings = {
    z: boolean;
    round: boolean;
};
export declare const DEFAULT_PRINT_SETTINGS: PrintSettings;
